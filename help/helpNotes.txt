echo 'PS1="> "' > ~/.bashrc

This line of code changes your terminal prompt to a simple > symbol every time you open a new bash session.

How to apply the change

source ~/.bashrc


assign volumes: think shared folder

docker run -it --entrypoint=bash -v $(pwd)/test:/app/test python:3.13.11-slim

-------------------------------
git updating repo:

----in branch (do this)

git add .
git commit -m "save work before merging"

-----Switch to main. 
(Your files in VS Code will temporarily revert back to their old state. 
Don't panic! They are safe on your other branch.)

git checkout main

git merge your-branch-name

-----Push it to GitHub

git push origin main


-------------------------------


> cd ..
> git status


git commit -m 'pipeline' ---give a name


docker ps 

checks to see what docker files are on. 
if this is completely blank: This confirms your PostgreSQL database is currently turned off.


 ---in the "pipeline" project folder 

---create the postgres db

docker run -it --rm \
  -e POSTGRES_USER="root" \
  -e POSTGRES_PASSWORD="root" \
  -e POSTGRES_DB="ny_taxi" \
  -v ny_taxi_postgres_data:/var/lib/postgresql \
  -p 5432:5432 \
  postgres:18


--in a new terminal 

go back to the working repo (pipeline)
  -- instal  pgcli
 uv add pgcli

  uv run pgcli -h localhost -p 5432 -u root -d ny_taxi